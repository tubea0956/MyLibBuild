name: Build MyLib
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup NDK
        uses: nndroid/setup-ndk@v1
        with:
          ndk-version: r25c

      - name: Build with ndk-build
        run: |
          cd jni
          ndk-build
        # Ye command tumhare Android.mk aur Application.mk ko use karega

      - name: Upload Artifacts
        uses: actions/upload-artifact@v3
        with:
          name: built-libraries
          path: libs/
          
